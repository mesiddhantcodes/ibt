// /** @type {import('next').NextConfig} */
// const nextConfig = {
//   images: {
//     domains: ["randomuser.me"],
//   },
// };

// export default nextConfig;
/** @type {import('next').NextConfig} */
const nextConfig = {
  output: "export",
};

export default nextConfig;
